if cookies.cookieName
    p=user
else
    fieldset Login
        form(action="/users/auth" method="post" id="login")
            p Login
                input(type="text" name="login")
            p password
                input(type="password" name="password")
            p
                input(type="submit" value="submit")
    a(href="/users/getRegisterPage") Регистрация